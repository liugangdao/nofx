# 快速参考

## 🚀 快速启动

```bash
# 1. 配置
cp tradingview_config.json.example tradingview_config.json
nano tradingview_config.json

# 2. 添加执行权限
chmod +x *.sh

# 3. 启动
./start.sh
```

## 📋 常用命令

| 命令 | 说明 |
|------|------|
| `./start.sh` | 启动服务 |
| `./stop.sh` | 停止服务 |
| `./restart.sh` | 重启服务 |
| `./status.sh` | 查看状态 |
| `./logs.sh -f` | 实时查看日志 |
| `./logs.sh -e` | 查看错误日志 |

## 🔧 Makefile 命令

```bash
make build      # 编译
make run        # 运行
make start      # 后台启动
make stop       # 停止
make restart    # 重启
make status     # 状态
make logs       # 查看日志
make chmod      # 添加执行权限
```

## 📡 测试命令

```bash
# 健康检查
curl http://localhost:9090/health

# 开多仓
curl -X POST http://localhost:9090/webhook \
  -H "Content-Type: application/json" \
  -d '{"action":"buy","symbol":"BTC"}'

# 平多仓
curl -X POST http://localhost:9090/webhook \
  -H "Content-Type: application/json" \
  -d '{"action":"close_long","symbol":"BTC"}'
```

## 📊 日志命令

```bash
tail -f tradingview_webhook.log          # 实时查看
tail -n 100 tradingview_webhook.log      # 最后100行
grep "error" tradingview_webhook.log     # 搜索错误
```

## 🔍 进程管理

```bash
# 查看进程
ps aux | grep tradingview_webhook

# 查看端口
netstat -tlnp | grep 9090

# 杀死进程
kill $(cat tradingview_webhook.pid)
```

## 🌐 网络配置

```bash
# 开放防火墙 (Linux)
sudo ufw allow 9090/tcp

# 查看公网IP
curl ifconfig.me

# 测试外部访问
curl http://YOUR_IP:9090/health
```

## 📝 配置文件

```json
{
  "port": 9090,
  "private_key": "your_key",
  "wallet_addr": "0xYourAddress",
  "testnet": false,
  "default_quantity": 0,
  "position_size_percent": 5.0,
  "default_leverage": 5,
  "webhook_secret": ""
}
```

## 🎯 TradingView 配置

**Webhook URL:**
```
http://YOUR_IP:9090/webhook
```

**Alert Message:**
```json
{
  "action": "buy",
  "symbol": "{{ticker}}"
}
```

## 🔐 安全建议

- ✅ 使用 `webhook_secret`
- ✅ 配置防火墙
- ✅ 使用 HTTPS（Nginx）
- ✅ 限制访问 IP
- ✅ 定期备份配置

## 📚 文档链接

- [快速开始](QUICK_START.zh-CN.md)
- [部署指南](部署指南.md)
- [网络配置](网络配置指南.md)
- [测试指南](测试指南.md)
- [Curl 示例](CURL_EXAMPLES.md)

## 🆘 故障排查

### 服务无法启动
```bash
# 查看日志
./logs.sh -e

# 检查端口
netstat -tlnp | grep 9090

# 检查配置
cat tradingview_config.json
```

### 无法外部访问
```bash
# 检查防火墙
sudo ufw status

# 检查监听地址
netstat -tlnp | grep 9090

# 测试本地
curl http://localhost:9090/health
```

### 查看详细错误
```bash
# 查看完整日志
cat tradingview_webhook.log

# 只看错误
./logs.sh -e

# 实时监控
./logs.sh -f
```

## 💡 提示

- 配置文件修改后需要重启服务
- 日志文件会持续增长，建议配置日志轮转
- 使用 systemd 可以实现开机自启
- 建议在测试网测试后再用主网
- 定期检查服务状态和日志
